export enum APIEndpoints {
  auth = 'auth',
  signIn = 'auth/signin',
  signUp = 'auth/signup',
  users = 'users',
  boards = 'boards',
  boardsSet = 'boardsSet',
  columns = 'columns',
  columnsSet = 'columnsSet',
  tasks = 'tasks',
  tasksSet = 'tasksSet',
  points = 'points',
}
