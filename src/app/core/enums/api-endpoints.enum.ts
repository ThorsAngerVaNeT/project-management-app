export enum APIEndpoints {
  signIn = 'auth/signin',
  signUp = 'auth/signup',
  users = 'users',
  boards = 'boards',
  boardsSet = 'boardsSet',
  columns = 'columns',
  columnsSet = 'columnsSet',
}
