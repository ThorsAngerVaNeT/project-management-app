import { createAction } from '@ngrx/store';

export const redirectToBoard = createAction('[Router] Redirect To Board');
export const redirectToRoot = createAction('[Router] Redirect To Root');
export const redirectToWelcome = createAction('[Router] Redirect To Welcome');
